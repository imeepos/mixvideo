请分析以下视频内容描述，并为其推荐最合适的文件夹：

视频内容描述：
{contentDescription}

可选文件夹：
{folderList}

请为每个文件夹评分（0-1），并说明匹配原因。返回JSON格式：
{
  "matches": [
    {
      "folderName": "文件夹名称",
      "score": 0.8,
      "reasons": ["匹配原因1", "匹配原因2"]
    }
  ]
}
